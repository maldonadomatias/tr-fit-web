// frontend/src/components/admin/alerts/AlertRowActions.tsx (stub — replaced in Task 10)
import type { CoachAlert } from '@/types/api';
export function AlertRowActions(_: { alert: CoachAlert }) {
  return <span>⋯</span>;
}
