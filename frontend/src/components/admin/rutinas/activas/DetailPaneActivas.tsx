export function DetailPaneActivas({ athleteId }: { athleteId: string }) {
  return (
    <div className="p-7 text-sm text-muted-foreground">
      Routine for {athleteId} — viewer coming in T17.
    </div>
  );
}
